# 问题2当前结果镜像

复现命令：

```powershell
.\run_problem.ps1 -ConfigPath q2_test\configs\current_result_mirror\config.yaml
```

Q1基线已冻结在本目录 `frozen_inputs/`。输出写入 `doc/results/q2/reproduced/`。该任务耗时较长，且当前方案未获全局认证。
