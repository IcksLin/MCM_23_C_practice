# 2024 年高教社杯全国大学生数学建模竞赛 C 题——问题 1—3 分析报告

> 阶段边界：本文只履行“建模手”职责，分析问题 1—3 的建模思路、数学定义、算法和验证合同；不求问题 3 数值解，不代替编程实现。本文是建模草稿，不能原样作为参赛作品提交。

## 1. 题目、附件与版本

目标题目已经由本地题面核验为“2024 年高教社杯全国大学生数学建模竞赛 C 题：农作物的种植策略”。本次读取的只读输入如下。

| 文件 | 内容 | SHA-256 |
|---|---|---|
| `C题/C题.pdf` | 1 页题面 | `C7B5E58BFF4189B8AFBA5505F7BFF7D4F08280FC291C51EF3F46134EBBF74F9A` |
| `C题/附件1.xlsx` | 54 个地块/大棚及 41 种作物的基本情况 | `5E98BF5E1B247624397E57E74759DA293BD72005DAA0E2DF3710A0DAC0E9EF6A` |
| `C题/附件2.xlsx` | 2023 年种植情况，以及亩产量、成本和价格区间 | `869081A3AB47D3BF8D0955106B622AAF0FD2C068FADA7948DA69B20EBF1D00CE` |
| `C题/附件3/result1_1.xlsx` | 情形 1 输出模板 | `4F2484C0D70A5C4D047163F2EE6EF486949E813330466F46DEF4BD7D98AF06AF` |
| `C题/附件3/result1_2.xlsx` | 情形 2 输出模板 | `6166D43F5A64BF9D1657E80D4AEE7F10F54BB1A5695B81A28A0AC5E657297649` |
| `C题/附件3/result2.xlsx` | 问题 2 输出模板；本次仅检查结构，不使用 | `6A1BA9FC28D14D0A4A795E5F0B7261FB6E32165517AFEE62BCD1931ABA5BEE8A` |

检查期间 `附件1.xlsx` 在工作区发生了一次外部保存：文件从初始 SHA-256 `B799A1...F0932`、17147 字节变为上表所列当前版本、35874 字节，修改时间为 2026-08-27 16:40:25。主 Agent 未保存或覆盖该附件。重新读取后，核心语义仍为 54 个地块/大棚、41 种作物和相同适种说明；变化表现为一个空白行被移除、部分数值单元格变成数值字符串。本文以当前哈希版本为冻结输入，并把显式数值类型转换加入预处理。

附件 1 中有 34 块露天耕地，共 1201 亩；另有 16 个普通大棚和 4 个智慧大棚，每个 0.6 亩，因此可配置对象共 54 个。作物共 41 种。结果模板按 2024—2030 七个年份、第一季/第二季、地块和作物填写种植亩数。

## 2. 问题 1 的结论目标

需要分别输出两套 2024—2030 年逐年、逐季、逐地块、逐作物的种植面积方案，并满足全部适种、季次、面积、轮作和管理约束：

1. 超过预期销量的产量完全滞销并浪费时，使七年总利润最大；
2. 超过预期销量的产量按 2023 年销售价格的 50% 出售时，使七年总利润最大。

题面未给出折现率，故主模型以七年名义利润之和为目标，不自行引入时间贴现。最终还应报告可行性、求解最优性界或 MIP gap，以及管理便利性指标，而不只给 Excel 面积表。

## 3. 数据理解与预处理计划

### 3.1 关键结构

| 地块类型 | 数量 | 面积合计 | 允许的种植结构 |
|---|---:|---:|---|
| 平旱地 | 6 | 365 亩 | 每年一季，粮食作物（水稻除外） |
| 梯田 | 14 | 619 亩 | 每年一季，粮食作物（水稻除外） |
| 山坡地 | 6 | 108 亩 | 每年一季，粮食作物（水稻除外） |
| 水浇地 | 8 | 109 亩 | 每年一季水稻，或两季蔬菜 |
| 普通大棚 | 16 | 9.6 亩 | 第一季普通蔬菜，第二季食用菌 |
| 智慧大棚 | 4 | 2.4 亩 | 两季普通蔬菜 |

豆类集合包括粮食豆类（作物 1—5）和蔬菜豆类（作物 17—19）。水浇地第二季仅允许大白菜、白萝卜、红萝卜；普通大棚第二季仅允许食用菌；智慧大棚两季均排除这三种根菜。

### 3.2 清洗规则

- 对作物名、地块类型和季次执行首尾空格清理；附件中“菠菜 ”、“生菜 ”和“普通大棚 ”存在尾随空格。
- 对面积、作物编号、亩产量、成本和价格端点执行显式数值转换；转换失败立即报错，不依赖 Excel 单元格显示格式推断类型。
- 将附件 1 当前版本字符串中的 `_x000d_` 还原为空格或换行；对“种植耕地”分组空白按合并区域传播值，再由作物编号和说明复核适种集合。
- 对附件 2 因 Excel 合并单元格产生的空白地块名向下填充，但不改写原文件。
- 附件 2 注明智慧大棚第一季的亩产量、成本和价格与普通大棚第一季相同、表中省略；因此对未来 18 种允许蔬菜建立参数继承映射，并用同一映射补齐 2023 年智慧大棚第一季的 6 条种植记录。若不继承，会漏计 12270 斤产量和 7080 元成本。
- 销售价格为区间时，以区间中点作为 2023 年基准价；区间上下端只进入敏感性复验。
- 由 2023 年种植亩数乘以对应地块类型、季次的亩产量，汇总得到作物—季次的 2023 年产量。题面未另给销量，主假设把该产量作为预期销量代理值。
- 主口径按“作物—季次”设置预期销量，因为题面写明每季作物当季销售；同时以“作物—全年合计”为替代口径做复验，防止对题意存在不同解释。
- 核对每个工作表首行、末行、有效行数、面积和季次；结果模板的表格形式不得修改。

### 3.3 数据质量判断

核心面积、作物编号、亩产量、成本和价格字段可用于建模，没有需要凭空补造的数值。需要特别处理的不是统计缺失，而是合并单元格和字符串空格。2023 年“实际销量等于产量”以及价格区间取中点均是模型假设，不是附件中的观测事实，必须做敏感性分析。

## 4. 假设及依据

| 级别 | 假设 | 依据 | 放宽后的复验 |
|---|---|---|---|
| 关键 | 2024—2030 的亩产量、亩成本、销售价和预期销量相对 2023 年稳定 | 问题 1 明确给定 | 不在问题 1 主模型内放宽；变化情形属于问题 2 |
| 关键 | 2023 年作物—季次产量可作为相应预期销量 | 题面未另给销量，附件给出 2023 种植及产量统计 | 将销量统一缩放为 90%、100%、110%，并比较年度聚合口径 |
| 关键 | 同一地块“所有土地三年内至少种一次豆类”等价为滚动三年豆类累计面积不少于该地块面积 | 题面强调“每个地块（含大棚）的所有土地” | 与“整块地至少一年全部种豆”这一更强口径对照 |
| 可放宽 | 销售价格区间以中点代表 2023 年价格 | 附件只给区间，没有单点成交价 | 分别用区间下端、上端求解或复算 |
| 可放宽 | 各要求种植的季次充分利用地块面积，不主动休耕 | 题目目标为充分利用有限耕地，2023 数据也按相应季次使用地块 | 把面积等式改为不等式并检查是否出现休耕 |
| 可放宽 | 不计运输、仓储、销毁和换茬成本 | 题面没有给出这些参数 | 以单位面积或单位产量附加成本做阈值扫描 |
| 可放宽 | 主阈值取 (\alpha_j=0.5A_j) | 2023 年 87 条非零记录的面积/地块面积只取 0.5 或 1.0，经验最小非零占比为 0.5 | 令 (\alpha_j=\eta A_j)，扫描 (\eta\in\{0.25,0.50,0.75,1.00\}) 和利润容忍度 |

## 5. 可用思路与算法：优先级 1—3

下表列出三条候选路线，但本题最终只选择优先级 1 的一个模型体系；优先级 2、3 仅在求解器或规模条件触发时回退，不与主模型叠加，因此不突破“每个子问题最多两个独立模型体系”的约束。

| 优先级 | 思路与算法 | 适配性 | 主要不足 | 结论 |
|---:|---|---|---|---|
| **1** | **全周期混合整数线性规划（MILP）+ 分支定界/分支割 + 分层（lexicographic）管理目标** | 收益、销量截断、面积、适种、轮作、三年豆类和管理逻辑均可精确线性化；可直接得到最优性界和 MIP gap | 0-1 变量较多，但按可行作物组合预筛后规模仍适合现代 MILP 求解器 | **主推并选用** |
| **2** | 面积离散化后的 CP-SAT/约束规划 | 禁连作、季次模式、豆类窗口等逻辑约束表达直接；对大量 0-1 逻辑常较稳健 | 必须把亩数和金额缩放为整数，离散粒度会引入误差；经济目标解释不如 MILP 自然 | MILP 数值表现差或无商用求解器时回退 |
| **3** | 可行性修复遗传算法或模拟退火 + 局部搜索 | 容易加入复杂非线性管理偏好，能较快找可行解 | 难保证轮作和豆类覆盖始终可行；没有可靠全局最优性证明，罚函数权重敏感 | 只作大规模应急或给 MILP 提供暖启动，不宜作为首选 |

不推荐把动态规划作为主算法：地块、作物、季次和销量上限跨地块耦合，状态维数会急剧膨胀，难以保持可解释性和可实现性。

## 6. 优先级 1：全周期 MILP 模型合同

### 6.1 集合与参数

- (j\in J)：地块或大棚；(i\in I)：作物；(t\in T=\{2024,\ldots,2030\})；(s\in S_j)：地块可用季次。
- (A_j)：地块面积，单位亩。
- (e_{jis}\in\{0,1\})：作物 (i) 是否适合地块 (j) 的季次 (s)。
- (q_{jis})：亩产量，单位斤/亩；(c_{jis})：种植成本，单位元/亩；(p_{is})：销售价格，单位元/斤。附件清洗后，同一作物—季次的可行地块组合价格一致。
- (D_{is})：由 2023 年数据估计的作物 (i) 在季次 (s) 的预期销量，单位斤。
- (L\subset I)：全部豆类作物集合。
- (\eta_0=0.5)：2023 年非零种植面积占地块面积的最小观测比例；主模型令 (\alpha_j=\eta_0A_j)。
- (\bar x^{2023}_{jis},\bar y^{2023}_{jis})：由附件 2 固定得到的 2023 年历史种植面积和启用状态，不属于未来决策变量。

### 6.2 决策变量

- (x_{jits}\ge 0)：地块 (j) 在年份 (t)、季次 (s) 种植作物 (i) 的面积，单位亩。
- (y_{jits}\in\{0,1\})：是否在对应地块、年份和季次启用作物 (i)。
- (u_{its}\ge 0)：作物 (i) 在年份 (t)、季次 (s) 以正常价格售出的数量，单位斤。
- (r_{jt}\in\{0,1\})：水浇地 (j) 在年份 (t) 是否采用单季水稻模式。

### 6.3 产量与销量截断

总产量为

\[
Q_{its}=\sum_{j\in J}q_{jis}x_{jits}.
\]

用以下线性约束表达正常价销量：

\[
0\le u_{its}\le Q_{its},\qquad u_{its}\le D_{is}.
\]

因为目标中 (u_{its}) 的系数为正，最优解自动满足 (u_{its}=\min(Q_{its},D_{is}))，不需要再引入分段函数或大 (M) 二进制变量。

### 6.4 两种滞销情形的目标函数

情形 1（超产浪费）：

\[
\max Z_1=\sum_{t,i,s}p_{is}u_{its}
-\sum_{j,i,t,s}c_{jis}x_{jits}.
\]

情形 2（超产部分按半价出售）：

\[
\max Z_2=\sum_{t,i,s}\left[\tfrac12p_{is}Q_{its}+\tfrac12p_{is}u_{its}\right]
-\sum_{j,i,t,s}c_{jis}x_{jits}.
\]

若后续清洗发现同一作物—季次存在不同销售单价，则必须按价格组设置产量与正常价销量变量，并用各价格组正常销量之和不超过 (D_{is})；不得直接使用未经定义的加权价格。

### 6.5 硬约束

1. **适种性与变量联动**

\[
0\le x_{jits}\le A_je_{jis}y_{jits},\qquad
x_{jits}\ge \alpha_jy_{jits}.
\]

2. **面积守恒**：平旱地、梯田和山坡地每年第一季的适种粮食面积和为 (A_j)；普通大棚第一季蔬菜、第二季食用菌面积分别为 (A_j)；智慧大棚两季普通蔬菜面积分别为 (A_j)。

3. **水浇地模式互斥**

\[
x_{j,\text{水稻},t,1}=A_jr_{jt},
\]

\[
\sum_{i\in I_{v1}}x_{jit1}=A_j(1-r_{jt}),\qquad
\sum_{i\in I_{v2}}x_{jit2}=A_j(1-r_{jt}),
\]

其中 (I_{v1}) 为第一季普通蔬菜集合，(I_{v2}=\{\text{大白菜、白萝卜、红萝卜}\})。附件要求第二季只能种三种根菜中的一种，因此还必须加入

\[
\sum_{i\in I_{v2}}y_{jit2}=1-r_{jt}.
\]

采用水稻模式时右端为 0；采用两季蔬菜模式时恰有一种第二季根菜被启用。结合面积守恒和变量联动，第二季不会混种两至三种根菜。

4. **禁止连续重茬**：对双季地块，约束同年第一季与第二季、上一年第二季与下一年第一季不能启用同一作物；对单季地块，约束相邻年份不能启用同一作物。水稻模式还需满足相邻年份不连续种水稻。2023 年实际种植使用固定历史状态 `bar_y_2023` 作为 2024 年边界条件，而不能只从 2024 年内部开始检查。

5. **滚动三年豆类覆盖**：对所有地块及所有完整三年窗口 (k,k+1,k+2\subseteq\{2023,\ldots,2030\})，要求

\[
\mathbf 1_{\{2023\in[k,k+2]\}}\sum_{s\in S_j}\sum_{i\in L}\bar x^{2023}_{jis}
+\sum_{\tau=\max(k,2024)}^{k+2}\sum_{s\in S_j}\sum_{i\in L}x_{ji\tau s}\ge A_j.
\]

其中 2023 年面积使用历史常量而非定义域外的决策变量。该约束比“任意一小块豆类即算一次”更符合“所有土地三年内至少种一次豆类”的原文。

6. **输出结构**：单季作物写入模板第一季对应单元格；所有不适种组合必须为 0；不得改变工作表名、行列和格式。

### 6.6 便于管理的分层目标

题面没有给出“不能太分散”和“面积不宜太小”的数值阈值。为避免任意加权，采用两阶段分层优化：

1. 先求最大利润 (Z^{*})；
2. 在 (Z\ge(1-\delta)Z^{*}) 下，最小化启用次数

\[
F=\sum_{j,i,t,s}y_{jits},
\]

并保留最小面积约束 (x_{jits}\ge\alpha_jy_{jits})。主结果先取 (\delta=0)，再对 (\delta\) 和 (\alpha_j) 做敏感性扫描，形成“牺牲少量利润换取更集中种植”的可解释选择。该处理仍属于同一 MILP 模型体系。

主阈值现冻结为 `alpha_j = 0.5 A_j`：2023 年 87 条非零种植记录的面积占地块面积比例只有 0.5 和 1.0，故 0.5 是可复现的经验最小非零占比。敏感性分析改用 `alpha_j = eta A_j`，其中 `eta` 依次取 0.25、0.50、0.75、1.00；原文中的“校准”均按此公式执行。

### 6.7 求解算法

1. 生成适种矩阵，删除全部不可能的 (x,y) 变量；规范化价格区间和 2023 历史状态。
2. 建立情形 1 的 MILP，以分支定界/分支割求 (Z_1^{*})，记录最优性界、MIP gap 和运行时间。
3. 固定利润容忍约束，最小化分散度 (F)，得到最终情形 1 方案。
4. 只替换收益表达式，按相同流程求情形 2；两情形共用全部可行域约束。
5. 将 (x) 映射至 `result1_1.xlsx` 和 `result1_2.xlsx`。本阶段只定义接口，不实际写入。

推荐求解器顺序：Gurobi/CPLEX（若有合法许可）→ SCIP/HiGHS MILP。PuLP 或 Pyomo 适合建模接口，但最终应报告底层求解器和版本。

## 7. 验证方案与完成标准

### 7.1 可行性审计

- 水浇地选择两季蔬菜模式时，第二季启用变量之和必须为 1，即只种大白菜、白萝卜、红萝卜中的一种；水稻模式时该和为 0。
- 主结果的每个非零面积必须达到 `0.5 A_j`；敏感性方案分别按 `eta A_j` 复核，其中 `eta` 取 0.25、0.50、0.75、1.00。
- 每个必种季次的面积和等于地块面积，误差不超过模板输出精度；所有面积非负。
- 不适种变量为 0，水浇地模式互斥，普通/智慧大棚季次作物类型正确。
- 逐地块扫描 2023—2030 时间序列，无相邻重茬。
- 每个滚动三年窗口豆类累计面积不少于地块面积。
- 每个非零种植面积达到 (\alpha_j)，并统计每种作物的地块启用数。

### 7.2 收益与最优性审计

- 独立复算 (Q,u)、正常价收入、半价收入、成本和总利润，不能只读取求解器目标值。
- 固定同一可行方案时，情形 2 的收入不得低于情形 1；若最优值不满足 (Z_2^{*}\ge Z_1^{*})，说明收益线性化或汇总口径有错。
- 当 (D\) 足够大时，两种情形应得到相同目标；当 (D=0) 时，情形 1 无销售收入、情形 2 只有半价收入。
- 精确求解优先达到 MIP gap 不高于 0.1%；若时间受限，必须同时报告 incumbent、best bound 和 gap，不得只称“最优”。

### 7.3 稳健性与消融

- 管理阈值按 `eta = 0.25, 0.50, 0.75, 1.00` 与利润容忍度联合扫描；0.50 为主结果。
- 销量：90%、100%、110% 三档；比较作物结构和利润变化。
- 价格：区间下端、中点、上端；检查排序是否剧烈翻转。
- 管理：扫描 (\alpha_j) 和 (\delta)，绘制利润—启用次数折衷关系。
- 口径：比较“作物—季次销量池”和“作物—年度销量池”。
- 消融：去掉管理次目标后比较分散度；去掉 2023 边界后检查是否产生表面可行但违反连续轮作的方案。

## 8. 编程依赖和候选图表

本阶段不写代码。后续实现可用 Python 3、pandas/openpyxl、Pyomo 或 gurobipy/SCIP 接口。建议候选图表包括：地块—年份—季次种植热力图、作物产量与销量上限对比图、七年利润分解图、滚动三年豆类覆盖裕度图、管理阈值的利润—集中度折衷图。

## 9. 风险与回退条件

| 风险 | 触发证据 | 回退处理 |
|---|---|---|
| 管理阈值敏感 | 小幅改变 `eta` 或利润容忍度即引起方案剧变 | 主结果固定数据支持的 `eta_0=0.5`，另报告折衷曲线和稳定区间 |
| 销量口径有歧义 | 季次汇总与年度汇总导致方案显著不同 | 并列报告两种解释，优先采用题面“每季”字面口径 |
| MILP 求解时间过长 | 预处理后仍无法在限时内取得可接受 gap | 先做对称性削减和变量筛选，再回退 CP-SAT 或用启发式暖启动 |
| 设施间销售价格不同 | 同一销量池混入不同价格后收入无法唯一分摊 | 按设施类型分组销量，或明确统一收购池与加权价格规则 |
| 轮作边界遗漏 | 2024 方案与 2023 实际种植同作物相邻 | 把 2023 决策固定进模型并重跑全部窗口 |

## 10. 文献依据与检索状态

1. Glen, J. J. (1987). *Feature Article—Mathematical Models in Farm Planning: A Survey*. Operations Research, 35(5), 641–666. DOI: [10.1287/opre.35.5.641](https://pubsonline.informs.org/doi/10.1287/opre.35.5.641)。该综述支持将农场规划视为运筹学数学规划问题；出版机构页面已核验。
2. Li, Q., Hu, G., Jubery, T. Z., & Ganapathysubramanian, B. (2017). *A farm-level precision land management framework based on integer programming*. PLOS ONE, 12(3), e0174680. DOI: [10.1371/journal.pone.0174680](https://pmc.ncbi.nlm.nih.gov/articles/PMC5367821/)。该文以 MILP 同时表达地块尺度、作物选择和现实资源约束，支持优先级 1 的模型族。
3. Alemany, M. M. E., Esteso, A., Ortiz, Á., & del Pino, M. (2021). *Centralized and distributed optimization models for the multi-farmer crop planning problem under uncertainty: Application to a fresh tomato Argentinean supply chain case study*. Computers & Industrial Engineering, 153, 107048. DOI: [10.1016/j.cie.2020.107048](https://www.sciencedirect.com/science/article/abs/pii/S036083522030718X)。该文直接讨论种植计划、供需不平衡、浪费和不确定性。

检索按技能要求尝试 OpenAlex 与 AnySearch 双引擎。AnySearch 返回上述候选，出版页/权威全文页又作了人工核验；OpenAlex 在两次请求中均因 SSL `UNEXPECTED_EOF_WHILE_READING` 失败，因此本次没有 DOI 双引擎交叉匹配记录，属于证据链限制而非模型结论。

## 11. 作者自检

- [x] 读完题面、附件 1、附件 2 和三个模板结构，原始输入保持只读。
- [x] 问题 1 已明确输出类型、数据、假设、模型、算法、验证和风险；问题 2 见第 12 章。
- [x] 三条路线已排序，但只选用一个主模型体系；回退路线不并行堆叠。
- [x] 变量、参数、目标函数、约束和单位已定义。
- [x] 管理便利性未使用无来源的固定权重，而采用分层目标和敏感性校准。
- [x] 水浇地第二季“三种根菜恰选一种”、智慧大棚第一季参数继承和 2023 历史常量已明确写入合同。
- [x] 给出可实现依赖、最优性标准、失败条件和输出接口。
- [x] 文献均保留 DOI 或可追溯页面；OpenAlex 失败已如实记录。

---

## 12. 问题 2：不确定条件下的风险厌恶种植规划

### 12.1 任务定义与结论目标

问题 2 要在问题 1 的土地、适种、季次、轮作、豆类覆盖和管理便利性约束全部保留的前提下，同时考虑 2024—2030 年预期销量、亩产量、种植成本和销售价格的不确定变化，输出唯一一套逐年、逐季、逐地块、逐作物种植面积方案至 `result2.xlsx`。最终结论不能只给期望利润，还应说明低收益尾部风险、相对确定性方案的收益代价，以及方案在未参与优化的新情景下是否稳定。

### 12.2 题面给定的动态范围与口径

令基期为 2023 年，`k=t-2023`。问题 2 不使用真实的 2024—2030 观测数据；以下变化均来自题面给定范围。

| 参数 | 题面信息 | 主模型情景生成口径 |
|---|---|---|
| 小麦、玉米预期销量 | 平均年增长 5%—10% | 每年独立抽取增长率 `g^D∈[0.05,0.10]`，按年复合递推 |
| 其他作物预期销量 | 相对 2023 年约有 ±5% 变化 | 各未来年取 `D_2023(1+eps^D)`，`eps^D∈[-0.05,0.05]`；不擅自复合漂移 |
| 亩产量 | 每年有 ±10% 变化 | `q_t=q_2023(1+eps^q)`，`eps^q∈[-0.10,0.10]` |
| 种植成本 | 平均每年增长约 5% | 主值 `c_t=c_2023(1.05)^k`；另以 4%—6% 年增幅作敏感性复验 |
| 粮食售价 | 基本稳定 | `p_t=p_2023` |
| 蔬菜售价 | 平均每年增长约 5% | `p_t=p_2023(1.05)^k`；另以 4%—6% 作敏感性复验 |
| 食用菌售价 | 年下降 1%—5% | 除羊肚菌外逐年抽取下降率并复合递推 |
| 羊肚菌售价 | 年下降 5% | `p_t=p_2023(0.95)^k` |

题目只给区间，没有给出概率分布和参数相关性；因此“区间内均匀抽样、参数暂按独立”只是**基准情景假设**，不是题目事实。问题 3 才明确要求考虑相关性，故问题 2 主模型不人为加入相关系数，但必须用端点压力测试和分布替换检验这一假设。

问题 2 没有重新说明超产处理方式。主结果采用保守口径：超过情景预期销量的产量无收入、按浪费处理；将“超产量按正常价 50% 销售”作为口径敏感性，不把二者混在同一个主结果中。

### 12.3 候选思路与优先级

本题评估两个候选模型体系，但本次只选择优先级 1 的随机 MILP 作为问题 2 模型；优先级 2 不进入本次求解、比较和 `result2.xlsx` 选择，因此当前交付只有一个实施模型体系。

| 优先级 | 模型体系 | 优点 | 风险与用途 |
|---:|---|---|---|
| **1** | **拉丁超立方情景生成 + 情景缩减 + 均值—下尾 CVaR 随机 MILP** | 直接利用题面区间；同时优化平均利润和最差一部分情景的平均利润；CVaR 可线性化，能继承问题 1 全部整数约束 | **主模型**；概率分布不是题面给定，必须做样本外和分布敏感性验证 |
| **2** | 预算不确定集鲁棒 MILP（Bertsimas–Sim 型） | 不要求精确概率分布，可控制同时发生多少个不利偏差 | **仅作后续回退候选，本次不选用、不要求编程手实现或与主结果比较**；销量截断的鲁棒对偶化需另立模型合同 |

不建议把遗传算法作为主模型：它能找方案，但难以稳定处理数万条硬约束，也不能直接给出风险目标的可靠上下界。蒙特卡洛模拟本身只负责制造和检验情景，并不自动产生最优种植方案。

### 12.4 主模型的集合、参数与变量

沿用问题 1 的地块集合 `J`、作物集合 `I`、年份集合 `T`、季次集合 `S_j`、面积 `A_j`、适种矩阵 `e_jis`、豆类集合 `L` 和 2023 历史边界。新增：

- `omega∈Omega={1,…,K}`：经缩减后的代表情景；`pi_omega` 为情景权重，且 `sum pi_omega=1`。
- `D_it_s^omega`：情景 `omega` 下的预期销量，斤。
- `q_jit_s^omega`：情景亩产量，斤/亩。
- `c_jit_s^omega`：情景亩成本，元/亩；主模型中成本按 5% 确定增长时可省略情景上标。
- `p_it_s^omega`：情景售价，元/斤。
- `x_jits,y_jits,r_jt`：与问题 1 相同，且**不带情景下标**。这表示竞赛需要的是一套事先确定、可写入 `result2.xlsx` 的计划，而不是每个未来情景一套互不相同的答案。
- `u_it_s^omega≥0`：情景正常价销量，斤。
- `Pi_omega`：方案在情景 `omega` 下的七年利润，元。
- `zeta∈R`：下尾利润分位辅助变量；`xi_omega≥0`：情景下行超额损失。

### 12.5 情景利润与销量线性化

情景产量为

\[
Q_{its}^{\omega}=\sum_{j\in J}q_{jits}^{\omega}x_{jits}.
\]

正常价销量满足

\[
0\le u_{its}^{\omega}\le Q_{its}^{\omega},\qquad
u_{its}^{\omega}\le D_{its}^{\omega}.
\]

当风险权重 `lambda<1` 时，目标中的期望利润正系数会推动 `u=min(Q,D)`；但在 `lambda=1` 的纯下尾目标中，非尾部情景的 `u` 可能非唯一。因此第一阶段风险目标完成后，必须增加“固定风险目标、最大化期望利润”的第二级字典序目标。最终风险指标还必须由固定 `x` 独立复算 `u=min(Q,D)`，不得直接信任可能退化的辅助变量。采用超产浪费主口径时，情景利润为

\[
\Pi_{\omega}=\sum_{t,i,s}p_{its}^{\omega}u_{its}^{\omega}
-\sum_{j,i,t,s}c_{jits}^{\omega}x_{jits}.
\]

若复验超产半价出售，则把收入替换为

\[
\sum_{t,i,s}\left[\tfrac12p_{its}^{\omega}Q_{its}^{\omega}
+\tfrac12p_{its}^{\omega}u_{its}^{\omega}\right].
\]

### 12.6 均值—下尾 CVaR 目标

设置信水平 `beta=0.90`，即关注利润最低的 10% 情景。下尾利润 CVaR 的线性表达为

\[
\operatorname{LCVaR}_{\beta}(\Pi)
=\zeta-\frac{1}{1-\beta}\sum_{\omega\in\Omega}\pi_{\omega}\xi_{\omega},
\]

其中

\[
\xi_{\omega}\ge \zeta-\Pi_{\omega},\qquad \xi_{\omega}\ge0.
\]

风险厌恶目标为

\[
\max Z_{\lambda}=(1-\lambda)\sum_{\omega}\pi_{\omega}\Pi_{\omega}
+\lambda\operatorname{LCVaR}_{\beta}(\Pi),
\qquad 0\le\lambda\le1.
\]

`lambda=0` 只追求期望利润，`lambda=1` 只追求低收益尾部表现。令 `lambda∈{0,0.1,…,1}` 形成风险前沿，将期望利润和下尾利润分别归一化，再计算各非端点到前沿两端连线的垂直距离。若最大距离至少为 0.02，则选择最大距离点；距离在 `1e-9` 内并列时依次选择较大的 `lambda`、较高的独立复算期望利润、较少的启用次数。若最大距离小于 0.02，视为无明显膝点，选择**满足下尾利润不低于前沿最大下尾利润 99% 的最小 `lambda`**，再按期望利润和启用次数破同分。该确定性规则唯一决定写入 `result2.xlsx` 的方案，同时保留三档方案供解释。

### 12.7 硬约束与管理次目标

所有农业约束与问题 1 完全一致，并且只对共同决策 `x,y,r` 建立一次：

1. 地块—作物—季次适种性和面积上下界；
2. 必种季次面积守恒；
3. 水浇地“单季水稻/两季蔬菜”互斥；
4. 含 2023 历史边界的禁止重茬；
5. 2023—2030 任意滚动三年豆类覆盖；
6. 最小种植面积和种植分散度控制。

为避免管理目标干扰风险收益解释，采用三级字典序顺序。定义金额容差

\[
\varepsilon(V)=\max\{1\text{ 元},10^{-6}|V|\},
\]

它只吸收浮点和求解容差，不表示经济偏好：

1. 在给定 `lambda` 下最大化 `Z_lambda`，得到 `Z_lambda^*`；
2. 加入 `Z_lambda≥Z_lambda^*-varepsilon(Z_lambda^*)`，最大化情景加权期望利润，得到 `E^*`。该级对所有 `lambda` 执行，特别用于消除 `lambda=1` 时正常销量变量的非唯一性；
3. 再加入 `E[Pi]≥E^*-varepsilon(E^*)`，最小化 `sum y_jits`，得到更集中的可执行方案。

不得把金额、启用次数和面积碎片直接用未经归一化的权重相加。

### 12.8 情景生成与缩减合同

1. 使用固定种子 2024 的 Latin Hypercube Sampling 生成 `N0=1000` 个原始情景；每个随机维度先在 `[0,1]` 分层抽样，再映射到题面区间。
2. 用问题 1 的 `result1_1` 固定方案快速复算每个原始情景的代理利润，将情景按代理利润分成 10 个等频层；最低利润层至少保留 `ceil(0.1K)` 个代表情景。各层内对标准化变化率向量使用固定种子的 PAM k-medoids 和 L1 距离，按该层样本数比例分配 medoid 数。代表情景必须是原始真实样本，权重为所属簇样本数除以 1000。这样既保留均值结构，也不会在缩减时系统性删除 10% 下尾。
3. 分别以 `K=20,30,50` 求解，比较目标值、作物总面积和下尾利润；若 `K=30` 到 `K=50` 的关键指标变化超过 2%，主模型提升到 50 个情景。
4. 对最终方案另生成至少 5000 个不参与优化的样本外情景，只做利润复算，不重新优化；报告均值、标准差、10%分位数、下尾 10%平均利润、最小利润和亏损概率。

`N0`、`K` 和 5000 不是题面常数，而是数值精度设置；必须把种子、样本规模、缩减权重和收敛结果写入复现清单。

### 12.9 未选回退候选：预算不确定集鲁棒优化

若后续证据表明概率假设不可接受，可另行建立预算鲁棒模型。其基本不确定集为

\[
a_k=\bar a_k+\hat a_k z_k,\qquad |z_k|\le1,
\qquad \sum_k|z_k|\le\Gamma.
\]

`Gamma` 控制同一时期内允许同时达到不利端点的参数数量。由于本题的 `min(Q,D)` 同时含不确定销量和亩产量，完整鲁棒对应需要额外补救变量、对偶化或外逼近，不能从上述集合直接声称得到可执行模型。因此它本次只作为失败后的重新建模方向，不进入编程合同、数值比较或最终方案选择。

### 12.10 验证、失败条件和输出要求

必须完成以下验证后才能把结果交给论文手：

- **逐情景复算**：独立复算 `Q,u,Pi`，并检查 `u≤Q,u≤D`；农业硬约束违约量为 0。
- **样本内/样本外分离**：优化情景与验证情景使用不同随机流；不得用同一批样本同时宣称优化和泛化有效。
- **基线比较**：主口径明确比较问题 1 的 `result1_1`、`lambda=0` 风险中性方案和最终风险方案；仅在超产半价敏感性中使用 `result1_2`。
- **压力测试**：销量取下端、亩产取下端、成本取高增长、价格取不利端，并构造联合不利情景；报告方案是否亏损及约束是否仍可行。
- **分布敏感性**：将均匀分布替换为同区间三角分布，并把题目只称“约”的 5%固定变化改作 4%—6%扫描。
- **稳定性**：更换至少 5 个随机种子；若推荐作物总面积、期望利润或下尾利润的变异系数超过 5%，增加情景数或改用鲁棒模型。
- **最优性**：报告 incumbent、best bound 和 MIP gap；未达到设定 gap 时只能称高质量可行方案。
- **模板**：`result2.xlsx` 含 2024—2030 七个年度工作表，保持模板工作表、行列、合并单元格和格式不变，只写种植面积。

### 12.11 给编程手的实现合同

建议在问题 1 已验证代码旁新建独立 `q2_test/`，不要在同一次改动中破坏问题 1。实现顺序如下。

1. **复用确定性可行域**：抽离问题 1 的集合、变量索引和农业硬约束为公共模块；先用单一中心情景证明 Q2 模型退化为确定性模型。
2. **参数生成器**：实现纯函数 `generate_raw_scenarios(base, n, seed)`，按 12.2 的递推规则返回长表，字段至少含 `scenario,year,plot,land_type,crop,season,demand,yield,cost,price`；其中销量和售价可在作物—季次层重复，亩产与成本必须保留地块或地类索引。对所有端点、索引唯一性和地类差异逐列断言。
3. **情景缩减器**：实现 `reduce_scenarios(raw, k, baseline_plan)`，按 12.8 的利润分层 PAM k-medoids 输出代表情景和权重；断言代表情景来自原样本、最低利润层代表数达标、权重非负且和为 1。
4. **随机 MILP 构造器**：`build_q2_model(md, scenarios, weights, beta, risk_lambda)`；`x,y,r` 不带情景下标，`u,Pi,xi` 带情景下标。先用 2 地块×2 作物×2 年×3 情景微型算例手算核对目标。
5. **求解顺序**：先跑 `K=5` 冒烟测试，再跑 `K=20/30/50` 收敛实验；每个 `lambda` 保存 incumbent、bound、gap、耗时和审计结果。可优先使用 Gurobi/COPT 的多线程与 MIP start；无许可证时用 HiGHS，但避免并行启动多个会争抢全部 CPU 的 MILP。
6. **风险前沿**：循环 `lambda=0…1`，输出 `risk_frontier.csv`；膝点选择代码必须只读取归一化后的期望利润与下尾利润，不人工挑最好看的点。
7. **样本外评估**：实现 `evaluate_fixed_plan(plan, scenarios)`，固定 `x` 后向量化计算至少 5000 个新情景，不调用 MILP；输出 `out_of_sample_metrics.csv` 和逐情景利润文件。
8. **模板回填**：不得用会重写未知页眉页脚的普通 `openpyxl.save()` 覆盖模板。先逐字节复制 `result2.xlsx` 为候选文件，再在 ZIP/OOXML 层只修改 7 个 `xl/worksheets/sheet*.xml` 的 `<sheetData>` 目标数值单元格；其他 ZIP 成员必须逐项 SHA-256 相同。对被改工作表比较移除 `<sheetData>` 后的规范化 XML，要求合并区域、尺寸、样式索引、页眉页脚、打印设置和工作表关系保持不变；随后执行逐单元格回读和约束审计，通过后原子替换正式结果。
9. **必备审计字段**：最大面积违约、最大适种违约、最小面积违约、重茬次数、豆类最小裕度、水浇地模式冲突、`max(u-Q)`、`max(u-D)`、利润复算差、CVaR复算差、Excel回读差。
10. **一键命令建议**：提供 `run_q2.py --seed 2024 --raw-scenarios 1000 --reduced-scenarios 30 --beta 0.90 --lambda-grid 0:1:0.1 --out-sample 5000 --template result2.xlsx`，并生成终端进度条、运行日志、依赖版本、输入 SHA-256 和唯一复现命令。

推荐输出除 `result2.xlsx` 外至少包括：`scenario_summary.csv`、`risk_frontier.csv`、`out_of_sample_metrics.csv`、`audit_q2.csv`、`repro_q2.json`，以及参数区间图、风险前沿图、样本外利润分布图、年度种植结构图和压力测试对比图。

### 12.12 问题 2 的风险与回退条件

| 风险 | 触发证据 | 回退处理 |
|---|---|---|
| 情景 MILP 过大 | `K=30` 在限时内没有可行解或内存明显不足 | 先缩减至 `K=10/20`、复用问题 1 解作 MIP start；仍失败再用 Benders/L-shaped 思路分解连续情景子问题 |
| 概率假设主导结论 | 均匀与三角分布下推荐结构明显翻转 | 停止当前编程交付，返回建模手另立完整预算鲁棒模型合同后再实现 |
| CVaR 权重无稳定膝点 | 前沿最大垂距小于 0.02 | 按 12.6 的“达到最大下尾利润 99% 的最小 `lambda`”规则唯一选择，同时报告三档方案 |
| 超产口径影响显著 | 浪费与半价出售下主要作物面积差异大 | 两种口径并列报告，主文声明保守口径，不能混算 |
| 求解 gap 偏大 | 限时后 gap 仍超过论文声明阈值 | 报告上下界，使用更强求解器、热启动和对称性削减，不声称全局最优 |

### 12.13 问题 2 文献依据

1. Rockafellar, R. T., & Uryasev, S. (2000). *Optimization of Conditional Value-at-Risk*. Journal of Risk, 2(3), 21–41. DOI: `10.21314/JOR.2000.038`。该文给出可通过线性辅助变量优化 CVaR 的经典表达。
2. Rockafellar, R. T., & Uryasev, S. (2002). *Conditional Value-at-Risk for General Loss Distributions*. Journal of Banking & Finance, 26(7), 1443–1471. DOI: `10.1016/S0378-4266(02)00271-6`。该文支持在有限离散情景下使用 CVaR，并说明其线性优化便利性。
3. Li, Q., Hu, G., Jubery, T. Z., & Ganapathysubramanian, B. (2017). *A farm-level precision land management framework based on integer programming*. PLOS ONE, 12(3), e0174680. DOI: `10.1371/journal.pone.0174680`。用于支持地块尺度整数规划结构。
4. Bertsimas, D., & Sim, M. (2004). *The Price of Robustness*. Operations Research, 52(1), 35–53. DOI: `10.1287/opre.1030.0065`。仅支持第 12.9 节的未选回退方向，不表示本次已经构造其完整鲁棒对应。

### 12.14 问题 2 作者自检

- [x] 已逐项覆盖销量、亩产量、成本、粮食价格、蔬菜价格和菌类价格变化。
- [x] 已区分题面区间、基准分布假设与数值精度参数。
- [x] 决策变量不随情景变化，最终能唯一映射到 `result2.xlsx`。
- [x] CVaR 方向为“提高低收益尾部利润”，公式符号与最大化目标一致。
- [x] 保留问题 1 的全部农业硬约束和 2023 历史边界。
- [x] 给出样本外、压力、分布、随机种子、场景数和最优性验证。
- [x] 只选择随机 MILP 作为实施模型；鲁棒 MILP 明确降级为未选回退方向，模拟和敏感性分析不计作独立模型。
- [x] 已给出可直接下放的模块、函数、字段、命令、审计和失败回退合同。

---

## 13. 问题 3：相关性、替代性与互补性下的联合随机规划

### 13.1 任务定义与结论目标

问题 3 要在问题 2 的模型和农业硬约束上进一步加入三类关系：作物间替代性、作物间互补性，以及预期销量、销售价格和种植成本之间的相关性。最终应输出 2024—2030 年逐地块、逐年、逐季的唯一种植策略，并在同一批相关样本外情景上与问题 2 方案作成对比较。

附件只含 2023 年单年截面，无法从中可靠估计 41 种作物的时间相关系数、交叉价格弹性或轮作增产率。因此这些量只能作为**透明、可复现的模拟参数**，不能写成由附件统计得到的事实。最终结论必须同时报告基准参数和弱—中—强三档敏感性结果；若方案随参数档位剧烈翻转，只能给出条件性建议。

### 13.2 候选路线与优先级

| 优先级 | 模型体系 | 作用 | 取舍 |
|---:|---|---|---|
| **1** | **因子相关矩阵 + t-Copula/秩保持 LHS + 交叉价格弹性 + 轮作互补线性化 + 均值—下尾 CVaR 随机 MILP** | 保留问题 2 的边际区间和可行域，同时显式生成联合相关情景；替代与互补机制可解释，仍可由 MILP 求解 | **选为主模型** |
| **2** | Wasserstein/矩约束分布鲁棒 MILP | 不依赖一个确定的相关矩阵，可对相关参数误设更稳健 | 需要严格构造模糊集并付出更高计算代价；仅在敏感性失稳时回退 |
| **3** | 系统动力学或多智能体仿真 + 启发式搜索 | 能描述价格反馈和市场主体互动 | 附件没有行为校准数据，参数不可识别且难给最优性界，不作为本题主路线 |

候选路线用于说明取舍；本次只建立优先级 1 的实施合同，不把三个模型叠加。

### 13.3 总体结构

主模型仍属于问题 2 的同一随机 MILP 模型族，只替换情景生成器并增加一个可线性化的互补收益项：

1. 按问题 2 的区间和递推规则生成每个随机变量的边际 LHS 样本；
2. 用因子结构构造半正定的 t-Copula 潜在线性相关矩阵，再按相关 t 分数的秩次序重排 LHS 样本；
3. 用交叉价格弹性矩阵把价格冲击转化为各作物预期销量变化；
4. 用豆类前茬激活变量表示题面明确提到的土壤互补效应；
5. 情景缩减后代入与问题 2 相同的均值—下尾 CVaR 三级字典序 MILP；
6. 用共同随机数对问题 2 与问题 3 方案作成对样本外评估，并做消融实验。

### 13.4 相关情景生成

设随机变化率向量为

\[
\mathbf v_{t}^{\omega}=
(\Delta D_{1t},\ldots,\Delta D_{|I|t},
 \Delta p_{1t},\ldots,\Delta p_{|I|t},
 \Delta c_{1t},\ldots,\Delta c_{|I|t},
 \Delta q_{1t},\ldots,\Delta q_{|I|t})^{\mathsf T}.
\]

不直接手填一个可能非半正定的高维相关矩阵，而采用因子结构构造 t-Copula 的**潜在线性相关矩阵**

\[
\mathbf z_t=L\mathbf f_t+\sqrt{\operatorname{diag}
(I-LL^{\mathsf T})}\,\boldsymbol\epsilon_t,
\qquad R_{\mathrm{lat}}=LL^{\mathsf T}
+\operatorname{diag}(I-LL^{\mathsf T}).
\]

要求 `L` 的每一行平方和不超过 1，从而剩余的特异方差非负。`R_lat` 不是 Spearman 矩阵。对任意两维 `a,b`，t-Copula 所对应的目标 Kendall 秩相关为

\[
\tau_{ab}^{*}=\frac{2}{\pi}\arcsin
\left((R_{\mathrm{lat}})_{ab}\right).
\]

生成后以样本 Kendall `tau_hat` 对照 `tau*`。审计对集合固定为：所有 `|tau*|>=0.10` 的非对角无序变量对，并强制并入“同一随机维度、相邻年份”的全部变量对；不得人工删减。原始样本在该集合上的最大绝对误差应不超过 0.05。缩减样本使用情景权重计算加权 Kendall，要求符号一致且最大绝对误差不超过 0.15，否则增加代表情景数。Spearman 只作描述性输出，不再与 `R_lat` 直接比较。

因子包括宏观成本因子、全局气候因子，以及每个附件作物类别各自的需求因子和气候因子。每个作物按附件唯一归入粮食、蔬菜或食用菌类别。各因子的七年高斯路径采用稳态初始化 `g_{k,2024}\sim N(0,1)`，并对 `t=2025,...,2030` 使用

\[
g_{k,t}=\rho_k g_{k,t-1}
+\sqrt{1-\rho_k^2}\,\nu_{k,t},
\qquad \nu_{k,t}\sim N(0,1).
\]

每个原始情景 `omega` 再只抽取一个 `S_omega~chi-square(nu)`，由该情景内**所有作物、参数和 2024—2030 七年共同使用**：

\[
z_{a,t}^{\omega}=\frac{
\sum_k L_{ak}g_{k,t}^{\omega}
+\sqrt{1-\sum_kL_{ak}^2}\,\epsilon_{a,t}^{\omega}}
{\sqrt{S_{\omega}/\nu}},
\qquad \epsilon_{a,t}^{\omega}\stackrel{iid}{\sim}N(0,1).
\]

这一定义冻结了卡方尺度的共享范围；特异项跨变量、跨年份独立，跨年相关只由各因子的 AR(1) 路径产生。完整七年块相关矩阵的元素因此为

\[
(R_{\mathrm{lat}})_{(a,t),(b,u)}=
\sum_k L_{ak}L_{bk}\rho_k^{|t-u|}
+\mathbf 1_{\{a=b,t=u\}}
\left(1-\sum_kL_{ak}^2\right).
\]

因子载荷的符号由机制确定：宏观因子同时推高多数作物成本和部分售价；有利气候因子推高同类作物亩产并压低其市场价格；类别需求因子使同类作物基础销量和售价同向变化。载荷和 `rho_k` 的弱—中—强档在 13.8 节冻结，全部是模拟设定。

为同时保留问题 2 扩展后的边际范围和相关秩结构，先生成相关 t 分数，再按每一维分数的秩对该维 LHS 样本重排。自由度取 `nu=5` 为基准，并以 `nu=8` 和高斯极限复验。若主要相关对的 Kendall 误差超出 0.05，则增加原始情景数或重新校准载荷。

问题 3 把问题 2 中仅作敏感性的“约”变化转为以下非退化边际，才能真正模拟销量—售价—成本依赖：成本逐年增长率取 `[4%,6%]` 并递推；蔬菜售价逐年增长率取 `[4%,6%]` 并递推；粮食售价相对当年趋势基线取 `[-1%,1%]` 的非累积扰动来表达“基本稳定”；普通食用菌仍按每年下降 `[1%,5%]` 递推。羊肚菌题面明确每年下降 5%，故其售价保持确定并从售价 Copula 维度中剔除。销量和亩产边际完全沿用问题 2：小麦、玉米销量递推，其他销量与亩产相对 2023 基准作非累积扰动。所有区间均需在输出中标明题面来源或模拟扩展身份。

### 13.5 作物替代性的交叉价格弹性模型

令 `D^{base,omega}_{its}` 为完成相关重排、但尚未施加价格弹性的基础预期销量；令 `bar_p_{its}` 为问题 2 在年份 `t`、季次 `s` 的确定趋势价格基线，`p^{omega}_{its}/bar_p_{its}` 才是该情景的随机价格冲击，不含蔬菜等作物的长期趋势。为避免与地块索引 `j` 冲突，另用 `h` 表示影响作物；设 `E^(s)=(e_ih^(s))` 为同一季次内的交叉价格弹性矩阵，则最终销量定义为

\[
D_{its}^{\omega}=D_{its}^{base,\omega}
\exp\!\left(
\sum_{h\in I_s} e_{ih}^{(s)}
\ln\frac{p_{hts}^{\omega}}{\bar p_{hts}}
\right).
\]

其中 `I_s` 是季次 `s` 中可销售的作物集合；不在同一季次出现的作物不设置弹性边。`e_ii<0` 表示本作物价格上升会压低自身销量；`e_ih>0` 表示作物 `i,h` 为替代品；`e_ih<0` 表示市场消费意义上的互补品。为防止 41 维矩阵过拟合，使用稀疏分组结构：同一用途或同一附件类别内只设置少量替代边，未获依据的跨组边固定为 0。稳定条件明确为

\[
\sum_{h\ne i}|e_{ih}^{(s)}|\le |e_{ii}^{(s)}|,
\]

即只对**非对角交叉项**求和，避免与非零交叉弹性自相矛盾。

附件无法识别真实弹性，故基准模拟只冻结符号和相对强弱，不宣称是真实市场估计。粮食、蔬菜、食用菌的基准自身价格弹性分别设为 `-0.25,-0.50,-0.60`；同类替代边在每一行的正弹性总和分别限定为 `0.15,0.20,0.25` 并在有效邻边间均分。没有题面或分组依据的市场互补边取 0，生产互补性由 13.6 节单独表达。上述数值整体乘以 `kappa in {0.5,1.0,1.5}` 扫描，均属于模拟设置。所有最终销量再检查非负性和相对基准的变化范围；若出现明显超出题意的变化，不截断后静默使用，而应减小弹性档位并记录触发原因。

### 13.6 作物互补性的轮作增益

题面明确说明豆类根瘤菌有利于其他作物生长，因此把这一条作为有题面依据的生产互补关系。令 `b_jt` 表示地块 `j` 在年份 `t` 的前一年度是否种过豆类，`w_jits=x_jits b_jt`。用以下线性约束代替乘积：

\[
0\le w_{jits}\le x_{jits},\qquad
w_{jits}\le A_j b_{jt},\qquad
w_{jits}\ge x_{jits}-A_j(1-b_{jt}).
\]

`b_jt` 由上一年度豆类启用变量的逻辑或确定。若 `Y_j,t-1^L` 是上一年度该地块全部豆类启用变量集合，则加入 `b_jt >= y`（对每个 `y in Y_j,t-1^L`）以及 `b_jt <= sum(Y_j,t-1^L)`；2024 年由 2023 历史记录固定。情景产量改为

\[
Q_{its}^{\omega}=
\sum_j q_{jits}^{\omega}
\left(x_{jits}+\gamma_i w_{jits}\right),
\]

其中非豆类作物 `gamma_i>=0`，豆类自身取 0。题目未给增产率，故只模拟 `gamma in {0,0.03,0.06}`。该二元触发假定“上一年只要启用豆类，就对下一年整块地发生同档增益”，会忽略豆类覆盖比例，因此只能作为可线性实现的强假设；必须另做 `gamma=0` 对照，若其主导方案则回退到按豆类面积比例分段线性化。该互补项不能替代原有滚动三年豆类硬约束；前者描述可能收益，后者保证题目要求。

### 13.7 联合随机 MILP 最终表达

农业硬约束、共同决策 `x,y,r`、情景正常销量 `u` 和 CVaR 变量完全沿用问题 2。相关情景下

\[
0\le u_{its}^{\omega}\le Q_{its}^{\omega},\qquad
u_{its}^{\omega}\le D_{its}^{\omega},
\]

\[
\Pi_{\omega}=
\sum_{t,i,s}p_{its}^{\omega}u_{its}^{\omega}
-\sum_{j,i,t,s}c_{jits}^{\omega}x_{jits}.
\]

下尾利润 CVaR 为

\[
\operatorname{LCVaR}_{\beta}(\Pi)
=\zeta-\frac{1}{1-\beta}
\sum_{\omega}\pi_{\omega}\xi_{\omega},
\quad
\xi_{\omega}\ge\zeta-\Pi_{\omega},\quad \xi_{\omega}\ge0.
\]

最终优化目标为

\[
\max Z_{\lambda}^{(3)}=
(1-\lambda)\sum_{\omega}\pi_{\omega}\Pi_{\omega}
+\lambda\operatorname{LCVaR}_{\beta}(\Pi).
\]

仍采用三级字典序：先最大化 `Z_lambda^(3)`，再在金额容差内最大化期望利润，最后最小化启用次数。价格是情景参数而不是决策变量，所以交叉弹性在建模前形成 `D_its^omega`，不会使 MILP 产生价格—销量双线性项；轮作增益通过 `w` 已线性化。

### 13.8 模拟参数与数值规模

每种作物 `i` 按附件类别映射 `g(i) in {粮食,蔬菜,食用菌}`。因子列固定为宏观因子 `M`、全局有利气候因子 `C`、三个类别需求因子 `G_g^D` 和三个类别气候因子 `G_g^q`。作物 `i` 只加载自身类别对应的列。基准载荷如下，未列载荷取 0：

| 随机维度 `a` | `M` | `C` | `G_g(i)^D` | `G_g(i)^q` | 特异项标准差 |
|---|---:|---:|---:|---:|---:|
| 成本增长率 | +0.60 | 0 | 0 | 0 | `sqrt(1-0.60^2)` |
| 基础销量变化率 | -0.10 | 0 | +0.50 | 0 | `sqrt(1-0.10^2-0.50^2)` |
| 价格变化率 | +0.30 | -0.30 | +0.35 | 0 | `sqrt(1-0.30^2-0.30^2-0.35^2)` |
| 亩产变化率 | 0 | +0.60 | 0 | +0.25 | `sqrt(1-0.60^2-0.25^2)` |

所有 `nu_k,t` 和 `epsilon_a,t` 均为相互独立的标准正态变量，只有 13.4 节定义的因子路径和每情景共同卡方尺度制造依赖。相关强度档位用 `kappa_R in {0.50,1.00,1.25}` 同时乘所有非零载荷；三档均保持每行平方和小于 1。所有共同因子的 AR(1) 系数统一设 `rho_k in {0.25,0.50,0.75}` 与三档一一对应；特异项不设跨年持续性。全部数值均是可复现的模拟设定，不是附件估计。

基准轮作增产率取 3%，并与 0%、6% 一起复验。弹性档位使用 13.5 节的 `kappa`，不与相关载荷乘子 `kappa_R` 混用。

- 随机种子：主种子 2024，另用至少 5 个种子复验；
- 原始相关情景：建议 `N0=2000`，先以 200 做 P1；
- 代表情景：先比较 `K=20,30,50`，缩减距离同时包含标准化参数向量、相关因子得分和代理利润；
- 风险水平：`beta=0.90`，`lambda=0,0.1,...,1`；
- 样本外情景：不少于 5000，并与问题 2 方案共享同一批随机数；
- 相关强度、交叉弹性和轮作增益均按弱—中—强档位扫描，不用一次模拟结果替代敏感性分析。

情景缩减除问题 2 的低利润层保护外，还应约束代表样本的相关矩阵失真；若 `K=30` 不能同时保留低尾和主要相关系数，则提升至 `K=50`。

### 13.9 与问题 2 的公平比较

比较必须使用四组消融方案：问题 2 独立情景基线、仅加入参数相关性、仅加入替代/互补机制、问题 3 全模型。问题 2 组直接读取已冻结的可行候选方案并只复算，不重新扫描 `lambda`；其余三组统一使用相同的 `beta`、`lambda` 网格与唯一方案选择规则、原始/缩减/样本外规模、求解时限和 MIP gap 门槛。消融属于同一模型的组件验证，不另计模型体系。

对每套固定方案，在同一批问题 3 样本外情景上计算：

1. 七年期望利润、标准差、10% 分位数、下尾 10% 平均利润和最小利润；
2. 亏损概率以及相对问题 2 的成对利润差 `Delta Pi_omega`；
3. 按年和作物类别的面积变化、总面积 L1 距离；
4. 激活组合 Jaccard 相似度、启用次数和种植集中度；
5. 相关性、弹性和 `gamma` 三类参数变化下推荐结构的稳定频率。

成对比较使用共同随机数，并对 `mean(Delta Pi)` 和下尾差做 bootstrap 区间。将“可接受平均利润损失”预先定义为：相对同批相关样本外情景中的问题 2 基线均值不超过 2%；同时报告 1%、3%、5% 阈值敏感性。只有当问题 3 下尾利润改善为正、其成对 bootstrap 95% 区间下界不小于 0，且平均利润损失满足上述门槛，才能称其更稳健；不能只比较各自在不同情景集上的求解器目标值。

### 13.10 给编程手的实现顺序

1. 从问题 2 复制独立 `q3_test/`，冻结问题 2 正式方案和输入哈希，不修改 `q2_test/` 权威结果。
2. 新建 `dependency.py`：以因子载荷生成半正定 `R_lat`，生成 t-Copula 秩并重排 LHS 边际；输出 `R_lat`、目标/样本 Kendall 矩阵、描述性 Spearman 矩阵及误差。
3. 新建 `elasticity.py`：建立作物分组、稀疏弹性矩阵和销量变换；断言对角线为负、替代边为正、互补边为负、行稳定条件满足。
4. 在模型中增加 `b,w` 及四条乘积线性化约束；用 2 地块、3 作物、2 年微型算例手算验证 `w=x*b` 和轮作增产。
5. 在相同农业可行域上跑四组消融；问题 2 固定方案只复算，不重新优化。
6. 先跑 `N0=200,K=5,lambda in {0,1}` 的最小闭环，通过约束、利润和相关矩阵审计后再进行正式扫描。
7. 正式运行输出相关矩阵、弹性矩阵、情景摘要、风险前沿、唯一方案、样本外成对利润、消融表和审计表；题目未指定问题 3 Excel 模板，故另存可读长表和一个保持 `result2.xlsx` 结构的候选工作簿，并在报告中明确其用途。

### 13.11 验证、失败条件与回退

| 风险/失败条件 | 判据 | 处理 |
|---|---|---|
| 相关矩阵不合法 | 最小特征值小于数值容差 | 不直接裁剪变量；回到因子载荷构造或最近相关矩阵投影 |
| 样本相关失真 | 主要非零相关对的样本 Kendall 与 `tau*` 最大绝对误差超过 0.05 | 增加 `N0`、调整载荷或提高 `K` |
| 弹性导致异常需求 | 小幅价格冲击造成需求数量级变化或负销量 | 降低弹性档位并检查单位；不得用无说明硬截断掩盖 |
| 方案参数敏感 | 弱—中—强档位下主要作物面积频繁翻转 | 只报告条件性策略，转入优先级 2 的分布鲁棒模型 |
| 互补增益主导结果 | `gamma=0` 与 3% 时方案即大幅翻转 | 不给单一推荐，报告阈值并降低互补机制结论强度 |
| 正式 MILP 未认证 | gap 超过声明阈值 | 只称高质量可行解，保留 incumbent/bound，不能声称全局最优 |

### 13.12 问题 3 文献依据

1. Iman, R. L., & Conover, W. J. (1982). *A distribution-free approach to inducing rank correlation among input variables*. Communications in Statistics—Simulation and Computation, 11(3), 311–334. DOI: `10.1080/03610918208812265`。用于支持在保留边际分布的同时施加目标秩相关结构。
2. Demarta, S., & McNeil, A. J. (2005). *The t Copula and Related Copulas*. International Statistical Review, 73(1), 111–129. DOI: `10.1111/j.1751-5823.2005.tb00254.x`。用于支持 t-Copula 的高斯尺度混合表示、潜在线性相关参数、Kendall 秩相关和尾部依赖。
3. Deaton, A., & Muellbauer, J. (1980). *An Almost Ideal Demand System*. American Economic Review, 70(3), 312–326。用于支持以自身价格和交叉价格关系刻画需求系统；本题因缺少时间序列不估计完整 AIDS，仅使用稀疏弹性结构进行模拟。
4. Alidoost, F., Su, Z., & Stein, A. (2019). *Evaluating the effects of climate extremes on crop yield, production and price using multivariate distributions: A new copula application*. Weather and Climate Extremes, 26, 100227. DOI: `10.1016/j.wace.2019.100227`。用于支持用 Copula 描述作物亩产、产量和价格的多变量依赖。
5. Alfandari, L., Plateau, A., & Schepler, X. (2017). *Mixed integer linear programming models for optimal crop selection*. Computers & Operations Research, 81, 26–39. DOI: `10.1016/j.cor.2016.12.004`。用于支持随机价格、随机亩产与 CVaR 下的作物组合 MILP。

双引擎论文脚本已按要求调用，但 OpenAlex 返回 HTTP 429、AnySearch 返回 SSL EOF，未形成交叉验证记录；上述候选另由出版机构、作者主页或期刊页面核验。该限制只影响双引擎交叉标记，不改变“参数必须作为模拟假设”的结论。

### 13.13 问题 3 作者自检

- [x] 明确区分了附件事实、经济机制和模拟参数，未声称从单年数据估计相关系数。
- [x] 替代性由交叉价格弹性表达，互补性至少有题面明确支持的豆类前茬增益。
- [x] 相关矩阵由因子结构保证半正定，边际范围仍沿用问题 2。
- [x] 决策变量保持跨情景共同，销量变换在优化前完成，互补乘积已线性化。
- [x] 比较使用共同随机数、同一问题 3 样本外情景和四组消融，避免不公平比较。
- [x] 给出参数扫描、相关失真、异常需求、求解 gap 和模型回退条件。
- [x] 主模型只有一个模型体系，其他路线仅作为失败回退。
