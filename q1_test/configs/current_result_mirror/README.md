# 问题1当前结果镜像

复现命令：

```powershell
.\run_problem.ps1 -ConfigPath q1_test\configs\current_result_mirror\config.yaml
```

输出写入 `doc/results/q1/reproduced/`，不会覆盖题目附件。当前镜像对应20秒未认证可行解；参考工作簿位于 `doc/results/q1/`。
